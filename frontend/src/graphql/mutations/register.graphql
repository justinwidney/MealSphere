mutation Signup($username: String!, $password: String!) {
  signupUser(data: { username: $username, password: $password }) {
    user {
      id
      username
    }
    token
    Errors {
      field
      message
    }
  }
}
